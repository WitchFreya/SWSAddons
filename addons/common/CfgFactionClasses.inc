class CfgFactionClasses {
  class NO_CATEGORY;
  class SWS_UNSC {
    META;
    displayName = QNAME(UNSC Scarlet Company);
    priority = 1;
    side = SIDE_BLUFOR;
    icon = QPATHTOEF(main,data\icon_sws_textless_ca.paa);
    flag = QPATHTOEF(main,data\flag_unsc_ca.paa);
    primaryLanguage = "EN";
  };

  class SWS_Utility : NO_CATEGORY {
    displayName = QNAME(Utility);
  };
};
